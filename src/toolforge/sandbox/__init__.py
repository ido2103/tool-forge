"""Sandbox: where all generated code runs.

Container-isolated execution with no network by default and per-tool
allowlisted domains. Logs every credential access and every execution.
Dev/testing uses throwaway accounts only.
"""
