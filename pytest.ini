[pytest]
testpaths = tests
addopts = -m "not live"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: exercises real endpoints (Anthropic API / local worker server); deselected by default. Run: uv run pytest -m live
